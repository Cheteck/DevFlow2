# Session Summary — 2026-08-08

## Goal

Create `.planning/PROJECT.md` for the MosaiX Theme System (brownfield GSD initialization) from the existing codebase analysis and PRD-0008.

## User Context

- MosaiX: composable application ecosystem (monorepo, pnpm, TS 5.7 ESM, strict).
- The Theme System is a generic theme resolution engine — it must be **agnostic of every business entity** (`space`, `store`, `organization`, `community`, … are all just `ThemeTarget = { type, id }`).
- Governing doc: `.project/prd/PRD-0008-theme-system.md` (now V2.3).
- User's vision: Shell + Governance (Control Plane) drive the theme; projects declare theming capabilities; business apps stay agnostic.

## Questions Asked & Resolved

1. **What this is (V1/V2/V3 vs plugin)** — Reframed from "plugin" to generic Theme System. Framework must not know business entities.
2. **1st release / MVP shape** — User's answer shifted `theme.tokens.get()` from "read-only", to "protected via registry" → then to full **Level 3 (write)** in the MVP. SDK boundary and registry capabilities (G3) were the hard points.
3. **User vs entity preference precedence** — Locked entity-first, user mode-only.
4. **Phase 1 scope** — Locked Core + SDK + Shell; UI package and Catalog → Phase 2+.
5. **Registry & UI location** — Locked catalog/preview/assignment UI in Control Plane, never kernel.
6. **Theme inheritance vs entity inheritance** — Two distinct mechanisms; entity inheritance Phase 2 + ADR.

## Key Decisions Made (2026-08-08)

| Decision                                                                 | Outcome          |
| ------------------------------------------------------------------------ | ---------------- |
| Phase 1 = Core + SDK + Shell                                             | ✓ Locked         |
| `ThemeTarget = { type, id }` generic                                     | ✓ Locked         |
| `CompositionContext.target` (not `themeTarget`)                          | ✓ Locked         |
| Entity-first precedence `entity > user > application > platform/default` | ✓ Locked (ADR-4) |
| Decoupled themeId vs mode resolution                                     | ✓ Locked         |
| Registry capabilities `userSelectable` / `adminConfigurable`             | ✓ Locked         |
| In-memory assignments store in Phase 1, port for Phase 2                 | ✓ Locked         |
| Catalog/preview/UI in Control Plane, never kernel                        | ✓ Locked         |
| Theme inheritance ≠ entity inheritance                                   | ✓ Locked         |
| Single `PolicyResolver` chain (ADR-0007) for authorization               | ✓ Locked         |

## Key Documentation

- `.planning/PROJECT.md` — written from PRD-0008 V2.3 + codebase analysis (ARCHITECTURE.md, STACK.md, CONCERNS.md).
- `.project/prd/PRD-0008-theme-system.md` — updated to V2.3 (resolved G/C/Q + ADR tables, footer).

## Work Product

- `.planning/PROJECT.md`
- `.project/prd/PRD-0008-theme-system.md` (V2.3)

## Blockers / Risks

- Legacy experience contracts in `@mosaix/contracts/src/experience/` predate ADR-0007 — must not be extended for theme.
- ADR-0007 (CompositionContext) is ratified but **not implemented** — theme implementation depends on it.
- `pnpm install` blocked locally by supply-chain policy (aws-sdk `minimumReleaseAge`); CI inactive (no remote).

## Next Steps

- Await user to run the roadmap creation phase (via `/gsd-new-milestone` / roadmap workflow) — I do NOT auto-trigger; this was the "Create PROJECT.md" GO step.
