# Session 2026-09-26 — Gestion DB façon Laravel (driver via .env, migrations/seeders via CLI)

## Objectif

Aligner la gestion base de données sur les pratiques Laravel : driver configuré depuis `.env` (`DB_CONNECTION`/`DB_*`), ports/adapters découplés via manager, migrations + seeders exécutés par le CLI (artisan) — jamais au boot.

## Travaux réalisés

- **Env** (`packages/core/src/env/env.schema.ts`) : clés `DB_CONNECTION` (`sqlite`|`pgsql`|`postgres`), `DB_DATABASE`, `DB_HOST`, `DB_PORT`, `DB_USERNAME`, `DB_PASSWORD` + `resolvedDbConnection` (explicite > inférence URL > `sqlite`). `.env.example` documenté (`check:env` OK, 50 clés). Tests : 3 nouveaux cas.
- **`@mosaix/database`** : `DatabaseManager` (connexions nommées, factories injectées, hook `onConnect`, `ready()`/`close()`) + `resolveDatabaseConfig()` pure et testée + `SQLITE_PRAGMAS`/`applySqlitePragmas()` partagés (fail-fast avec `cause`). 9 tests.
- **`@mosaix/migrations`** : `SqlMigrationStore` persistant dialect-aware (`?`/`$n`), exporté.
- **Shell** : `ShellCoreMigrationProvider` (`shell.core.v1.001`, DDL historique à l'identique, + down), registre agrégé `createShellMigrationRegistry()`, `runShellMigrations()` sur registre complet + `getPendingMigrationIds()` lecture seule (boot verifier), `DatabaseSeeder` CLI-only dialect-aware, `database-bootstrap.ts` réduit à connexion + PRAGMAs awaited + `databaseReady()`/`closeDatabase()`.
- **CLI** : `migrate` / `migrate:status` / `migrate:rollback [--steps=N]` sur manager + store persistant + registre agrégé, nouveau `db:seed`, `migrate --seed`, fermeture des connexions (`manager.close()` en `finally` — requis sous Windows). Helper partagé `database-command.ts`. 4 tests router.
- **Boot** (`src/start.ts`) : connect + verify fail-fast (`pnpm mosaix migrate` requis), plus de DDL/seed au boot. `feed-service` : constructeur `DatabasePort`, seed à l'import supprimé. `db:setup` = migrate + seed + rapport, dialect-aware.
- **Améliorations vs HEAD** : `{ cause: err }` (règle eslint `preserve-caught-error`), formatage prettier conforme au gate.

## Décisions prises

- Boot ne migre jamais (fail-fast même en dev, message actionnable) — fidélité Laravel (`artisan migrate` avant `serve`).
- `DB_CONNECTION` défaut `sqlite` (comportement dev actuel préservé) ; `pgsql` sans factory = erreur explicite (pas de driver `pg` bundlé).
- `BookingService` seed-en-constructeur conservé (test en dépend) → backlog DB-BOOKING-SEED.
- Pas de commit (non demandé).

## Validation effectuée

- `pnpm check:env` OK ; `pnpm test` final : 128 fichiers / 746 tests verts (dont 4 nouveaux tests CLI artisan-flow, 3 env DB_CONNECTION, 2 seeder) ; ESLint propre ; prettier appliqué.
- ESLint propre sur fichiers touchés ; prettier appliqué.
- Non effectué : `pnpm db:setup` réel sur `data/` (couvert par tests sur DBs temporaires, évite de muter la DB dev) ; e2e Postgres (pas de driver).

## Risques détectés

- `.env` racine pointe `MOSAIX_DATABASE_URL` vers Postgres alors que le boot effectif est SQLite sans `DB_CONNECTION` explicite → documenté, à trancher (DB-PGSQL-DRIVER).
- Tables sans FK (`foreign_keys=ON` inopérant) → backlog DB-FK-HARDEN.
- Imports relatifs CLI → `src/shell` → backlog DB-SHELL-REGISTRY-PKG.
- Schémas par-BAC toujours centralisés dans `shell.core.v1.001` → backlog DB-BAC-OWNERSHIP.

## Prochaines actions

DB-PGSQL-DRIVER et DB-BAC-OWNERSHIP en priorité pré-prod (voir backlog §11).

## Git State

- Branche active : `main`
- Commit avant : `015f6fa` (socle déjà présent en HEAD, arbre démarré régressé puis restauré/convergé)
- Commits créés : aucun (travail non committé, à la demande implicite — validation humaine requise avant commit)
- Fichiers : 15 modifiés vs HEAD (deltas bénins : prettier + `cause` + tests), backbone déjà présent en HEAD
- Rollback disponible : `git stash` / `git checkout -- <fichier>`
