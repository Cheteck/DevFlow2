{
  "name": "@mosaix/core",
  "version": "0.1.0",
  "description": "MosaiX Runtime Kernel — lifecycle, registries, communication backbone",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "rimraf dist",
    "bench": "vitest bench"
  },
  "files": [
    "dist"
  ],
  "eslintBoundaries": {
    "type": "core"
  },
  "dependencies": {
    "@mosaix/config": "workspace:*",
    "@mosaix/contracts": "workspace:*",
    "@mosaix/container": "workspace:*",
    "@mosaix/http": "workspace:*",
    "@mosaix/ports-database": "workspace:*",
    "@mosaix/schemas": "workspace:*",
    "@mosaix/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}