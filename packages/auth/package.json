{
  "name": "@mosaix/auth",
  "version": "0.1.0",
  "description": "MosaiX Authentication bounded context — providers, sessions, challenges, tokens",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "rimraf dist"
  },
  "files": [
    "dist"
  ],
  "eslintBoundaries": {
    "type": "core"
  },
  "dependencies": {
    "@mosaix/contracts": "workspace:*",
    "@mosaix/ports-credential-store": "workspace:*",
    "@mosaix/ports-id": "workspace:*",
    "@mosaix/ports-identity-store": "workspace:*",
    "@mosaix/ports-session-creation": "workspace:*",
    "@mosaix/ports-session-store": "workspace:*",
    "@mosaix/ports-token-store": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}