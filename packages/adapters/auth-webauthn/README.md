# @mosaix/adapter-auth-webauthn

Official MosaiX adapter implementation for `auth-webauthn`.

## Overview
This package implements the hexagonal port contract for `auth-webauthn`, bridging platform primitives with infrastructure drivers.

## Status
- **Implementation State**: Production Ready / Fully Conforming
- **Layer**: Layer 3 (`framework-adapter`)
