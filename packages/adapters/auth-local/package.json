{
  "name": "@mosaix/adapter-auth-local",
  "version": "0.1.0",
  "description": "Local password authentication provider for MosaiX",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "eslintBoundaries": {
    "type": "adapters"
  },
  "dependencies": {
    "@mosaix/contracts": "workspace:*",
    "@mosaix/ports-credential-store": "workspace:*",
    "@mosaix/ports-identity-store": "workspace:*",
    "@mosaix/ports-secrets": "workspace:*",
    "@mosaix/ports-session-creation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}