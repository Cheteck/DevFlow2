# @mosaix/adapter-database-sqlite

Official MosaiX adapter implementation for `database-sqlite`.

## Overview
This package implements the hexagonal port contract for `database-sqlite`, bridging platform primitives with infrastructure drivers.

## Status
- **Implementation State**: Production Ready / Fully Conforming
- **Layer**: Layer 3 (`framework-adapter`)
