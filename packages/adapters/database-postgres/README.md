# @mosaix/adapter-database-postgres

Official MosaiX adapter implementation for `database-postgres`.

## Overview
This package implements the hexagonal port contract for `database-postgres`, bridging platform primitives with infrastructure drivers.

## Status
- **Implementation State**: Production Ready / Fully Conforming
- **Layer**: Layer 3 (`framework-adapter`)
