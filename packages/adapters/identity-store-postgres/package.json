{
  "name": "@mosaix/adapter-identity-store-postgres",
  "version": "0.1.0",
  "description": "PostgreSQL adapter for IdentityStore port",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "eslintBoundaries": {
    "type": "adapters"
  },
  "dependencies": {
    "@mosaix/ports-identity-store": "workspace:*",
    "@mosaix/ports-database": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "rimraf": "^5.0.0"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}