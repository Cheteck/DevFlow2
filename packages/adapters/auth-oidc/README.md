# @mosaix/adapter-auth-oidc

Official MosaiX adapter implementation for `auth-oidc`.

## Overview
This package implements the hexagonal port contract for `auth-oidc`, bridging platform primitives with infrastructure drivers.

## Status
- **Implementation State**: Production Ready / Fully Conforming
- **Layer**: Layer 3 (`framework-adapter`)
