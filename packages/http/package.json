{
  "name": "@mosaix/http",
  "version": "0.1.0",
  "description": "MosaiX HTTP & Controllers SDK — Controller, Router, HttpRequest, and HttpResponse primitives",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "eslintBoundaries": {
    "type": "http"
  },
  "dependencies": {
    "@mosaix/contracts": "workspace:*",
    "@mosaix/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
